// Define the package to use
package main

// Import the fmt package
import "fmt"

// Call the main function, defines two string variables using shorthand and outputting each to the terminal on a seperate line
func main() {

	name := "Justin"
	moved := "06/2025"

	fmt.Println(name)
	fmt.Println(moved)

}
