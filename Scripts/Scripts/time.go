// This defines the package to use
package main

// This imports the fmt package
import "fmt"

// This assigns the string to the time variable and then prints the output to the terminal when it is called
func main() {
	var time string = "It is Saturday, September 6th when I wrote this"
	fmt.Println(time)
}
